#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(void) {
  for(int i=0;i<10;i++){
    int* a1 = malloc(1048576);
    memset(a1,0,4);
    sleep(1);
  }
//si and so fields always have zero values when executing the program with vmstat 1
  return 0;
}
